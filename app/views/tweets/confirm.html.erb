<div class="section">
	<div class="container">
	<div class="columns is-centered">

		 <div class="column is-4">

		<h2 class="title is-2">Confirmation Page</h2>


<%= form_with(model: @tweet, url: tweets_path, local:true) do |form| %>
    <%= form.hidden_field :tweet %>
    <p class="title is-5"> Your Tweet: <%= @tweet.tweet %></p>
  <%= form.submit  "Submit" %>
<% end %>

<%= form_with(model: @tweet, url: new_tweet_path, local:true,method: 'get') do |form| %>
    <%= form.hidden_field :tweet %>
  <%= form.submit  "back", name: 'back' %>
<% end %>

     </div>

    </div>
   </div>
  </div>
